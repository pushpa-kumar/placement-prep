{
  "intro": "These techniques extend the Arrays & Strings toolbox with structural tricks for range updates, in-place multi-way partitioning, and windowed extrema, while Merge Intervals extends the Sorting & Binary Search idea of 'sort first, then sweep' to problems whose objects are ranges rather than points.",
  "subtopics": [
    {
      "name": "Difference Arrays",
      "theory": "A difference array turns range-update problems into point-update problems. Given an array a[1..n], define d[1] = a[1] and d[i] = a[i] - a[i-1] for i > 1 (conceptually d[0] = a[0] = 0). The key identity is that a[i] = d[1] + d[2] + ... + d[i], i.e. a is the prefix-sum of d. This means adding a constant v to every element of a contiguous range a[l..r] can be done in O(1) by only touching the difference array: d[l] += v and d[r+1] -= v (if r+1 <= n). After all range updates are applied to d, a single prefix-sum pass over d reconstructs the final array a in O(n). This is the array analogue of a Fenwick/segment tree range-update, but with no logarithmic factor -- it is only valid when all updates are applied before any point is read back (an 'offline' batch of updates), which is exactly the situation in most of these problems.\n\nThe deeper and more powerful insight is the reverse direction: reconstructing the MINIMUM number of range-add operations that could have produced a given final array, starting from all zeros. Since each individual operation rangeAdd(l, r, +1) changes exactly two entries of the difference array (d[l] += 1, d[r+1] -= 1), and a value v > 0 at position i of d must be 'created' by v operations whose left endpoint is i (each contributing +1), the minimum number of operations equals the sum of all POSITIVE values in the difference array. This is because every operation contributes exactly one positive unit somewhere in d and exactly one negative unit (or falls off the end); an optimal strategy greedily starts a new range exactly when d[i] is positive, and closes ranges exactly when d[i] is negative, and it is never beneficial to start more ranges than the total positive mass requires.\n\nThis reconstruction insight generalizes further: when updates can be signed (+1 or -1 operations, as in turning nums into target), split the required delta d[i] = target[i]-nums[i] into a non-negative part (dpos[i] = max(0, d[i])) and a non-positive part (dneg[i] = max(0, -d[i])). Because a +1 range operation and a -1 range operation can never be merged into fewer operations (they move the value in different directions), the two parts are independent sub-problems, each solved by the same 'sum of positive jumps in the difference array' rule, and the answers simply add.\n\nDifference arrays also compose naturally with other structures: 2D difference arrays extend the same trick to rectangle-add/point-query grids, and combining a difference array with a Fenwick tree (a 'Fenwick tree of differences') supports range-add + range-sum-query in O(log n) per operation when updates and queries are interleaved online rather than batched.",
      "recognition": "The problem describes many operations of the form 'add v to every element in range [l, r]' (or 'increment/decrement a contiguous subarray'), especially when all updates happen before you need to read the final array; or the problem gives you a final array and asks for the minimum number of range-add operations that could have produced it from all zeros.",
      "complexity": "O(n + q) time and O(n) space for q batched range updates followed by one reconstruction pass; O(1) per update, O(n) for the final prefix-sum reconstruction.",
      "examples": [
        {
          "title": "Minimum Range-Increment Operations to Reach a Target Array",
          "problem": "You start with an array of n zeros. An operation picks a contiguous range [l, r] (1 <= l <= r <= n) and adds 1 to every element in that range. You are given the final array b[1..n] that resulted from applying some sequence of such operations. Find the minimum possible number of operations that could have produced b.\n\nInput: n, then n integers b[1..n] (0 <= b[i] <= 10^9).\nOutput: the minimum number of operations.",
          "approach": "Build the difference array d[i] = b[i] - b[i-1] for i = 1..n, treating b[0] = 0. Every operation rangeAdd(l, r) increases d[l] by 1 and decreases d[r+1] by 1 (or has no effect on the 'far side' if r = n). Because each operation contributes exactly one unit of positive mass to some d[l], the total number of operations needed is at least the sum of all positive entries of d; and this bound is achievable by, at each position i where d[i] > 0, opening d[i] new ranges starting at i, and at each position where d[i] < 0, closing that many ranges (always possible greedily since the array of currently-open ranges never goes negative -- this follows because b[i] = sum of d[1..i] >= 0 for all i, guaranteeing enough open ranges exist to close). So the answer is simply the sum of max(0, d[i]) over i = 1..n. This runs in O(n) with O(n) space for the difference computation.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; cin >> n;\n    vector<long long> b(n+1, 0);\n    for (int i = 1; i <= n; i++) cin >> b[i];\n    long long ops = 0;\n    for (int i = 1; i <= n; i++) {\n        long long d = b[i] - b[i-1];\n        if (d > 0) ops += d;\n    }\n    cout << ops << endl;\n}\n",
          "sample_input": "5\n1 2 2 3 1\n",
          "expected_output": "3\n"
        },
        {
          "title": "Minimum Operations to Turn nums into target (signed range updates)",
          "problem": "You are given two integer arrays nums and target, both of length n. In one operation you may choose a contiguous subarray of nums and either add 1 to every element in it, or subtract 1 from every element in it. Find the minimum number of operations needed to make nums equal to target.\n\nInput: n, then n integers nums[1..n], then n integers target[1..n].\nOutput: the minimum number of operations.",
          "approach": "Let d[i] = target[i] - nums[i] be the required signed delta at each position. A +1 range operation can only ever help positions that still need a positive delta, and a -1 range operation can only help positions that still need a negative delta -- the two kinds of operations never interact usefully in the same range, so the problem decomposes into two independent instances of the classic difference-array reconstruction: dpos[i] = max(0, d[i]) needs to be built with +1 range ops, and dneg[i] = max(0, -d[i]) needs to be built with -1 range ops. Each is solved exactly like the previous example: form the difference array of dpos (respectively dneg) with an implicit 0 before index 0, and sum the positive jumps. The final answer is the sum of the two independent costs. This is O(n) time and space.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nlong long costOneDirection(vector<long long>& d) {\n    // d[i] >= 0 required contribution at each index; cost = sum of positive jumps\n    // treat as difference array with implicit d[-1] = 0\n    long long ops = 0;\n    long long prev = 0;\n    for (long long x : d) {\n        long long jump = x - prev;\n        if (jump > 0) ops += jump;\n        prev = x;\n    }\n    return ops;\n}\nint main(){\n    int n; cin >> n;\n    vector<long long> nums(n), target(n);\n    for (auto &x : nums) cin >> x;\n    for (auto &x : target) cin >> x;\n    vector<long long> dpos(n), dneg(n);\n    for (int i = 0; i < n; i++) {\n        long long d = target[i] - nums[i];\n        dpos[i] = max(0LL, d);\n        dneg[i] = max(0LL, -d);\n    }\n    long long ans = costOneDirection(dpos) + costOneDirection(dneg);\n    cout << ans << endl;\n}\n",
          "sample_input": "4\n3 5 1 2\n4 6 2 4\n",
          "expected_output": "2\n"
        }
      ]
    },
    {
      "name": "Two-Pointer Partitioning (Dutch National Flag)",
      "theory": "The Dutch National Flag problem (Dijkstra) asks how to sort an array containing only three distinct values (conventionally 0, 1, 2) in-place in a single O(n) pass, without counting sort. The classic solution maintains three regions using three pointers: low marks the boundary of the region known to contain only 0s (everything in [0, low) is 0), high marks the boundary of the region known to contain only 2s (everything in (high, n) is 2), and mid scans forward through the unknown middle region. At each step, look at a[mid]: if it's 0, swap it with a[low] and advance both low and mid (the swapped-in element at position low was already known to be a 1 or already processed, so mid can safely advance too); if it's 1, it's already in its correct relative region, so just advance mid; if it's 2, swap it with a[high] and decrement high WITHOUT advancing mid, because the element swapped in from position high has not yet been examined and could be 0, 1, or 2.\n\nThe general pattern -- maintaining multiple pointers that each mark the boundary of a region with a known invariant, and advancing them based on a local test -- generalizes far beyond three-way sorting. It applies to any 'in-place partition into k labeled groups' problem, to problems that require partitioning a sorted or unsorted array into a 'low group' and a 'high group' related by some transformation (e.g. recovering an original array from a k-shifted interleaving), and to multi-region search problems where the boundaries of several valid segments must be discovered simultaneously by scanning with coordinated pointers rather than nested loops.\n\nA second, related pattern is 'matching a multiset against a candidate transformation': given a shuffled combination of two related sequences (e.g. {v-k, v+k} pairs for unknown k), sort the combined multiset, then repeatedly peel off the smallest remaining value x and require its designated partner value (x plus some offset determined by the candidate k) to also be present; if this greedy peeling matches every element exactly once, the candidate k is validated and the matching directly reconstructs the original array. This only works correctly when applied to the SORTED order (so that 'smallest remaining element' always corresponds to the 'low' member of a pair, never a 'high' member whose partner has already been consumed) -- this is what makes it a genuine two-pointer/partition technique rather than arbitrary multiset matching.",
      "recognition": "The array (or the target structure) has a small, fixed number of distinct categories/labels that must end up in contiguous, ordered regions in one pass (Dutch flag itself); or the array was formed by combining two related groups of values under some unknown shift/scale, and you must partition the sorted values back into a 'low' group and a 'high' group that pair up exactly.",
      "complexity": "O(n) time, O(1) extra space for the classic three-way in-place partition; O(n log n) time (dominated by the sort) for multiset-matching reconstruction problems.",
      "examples": [
        {
          "title": "Sort an Array of 0s, 1s, and 2s (Dutch National Flag)",
          "problem": "Given an array of n integers, each equal to 0, 1, or 2, sort the array in-place in a single pass (O(n) time, O(1) extra space), without using a library sort or counting sort.\n\nInput: n, then n integers (each 0, 1, or 2).\nOutput: the sorted array, space-separated.",
          "approach": "Maintain three pointers: low (next position to place a 0), mid (current element under examination), and high (next position to place a 2), initialized to 0, 0, n-1. Loop while mid <= high: if a[mid] == 0, swap a[low] with a[mid], then increment both low and mid (the element now at mid, previously at low, is guaranteed to be a 1 or already-examined 0, safe to skip past). If a[mid] == 1, just increment mid (it's already correctly placed in the middle region). If a[mid] == 2, swap a[mid] with a[high] and decrement high, but do NOT advance mid, since the newly swapped-in value at mid has not yet been classified. The invariant maintained throughout is: [0, low) contains only 0s, [low, mid) contains only 1s, [mid, high] is unexamined, and (high, n) contains only 2s. When mid exceeds high, the unexamined region is empty and the array is fully sorted.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; cin >> n;\n    vector<int> a(n);\n    for (auto &x : a) cin >> x; // each x in {0,1,2}\n    int low = 0, mid = 0, high = n - 1;\n    while (mid <= high) {\n        if (a[mid] == 0) {\n            swap(a[low], a[mid]);\n            low++; mid++;\n        } else if (a[mid] == 1) {\n            mid++;\n        } else { // a[mid] == 2\n            swap(a[mid], a[high]);\n            high--;\n            // do not advance mid: the swapped-in value must still be examined\n        }\n    }\n    for (int i = 0; i < n; i++) cout << a[i] << (i+1<n?' ':'\\n');\n}\n",
          "sample_input": "6\n2 0 2 1 1 0\n",
          "expected_output": "0 0 1 1 2 2\n"
        },
        {
          "title": "Recover the Original Array from a k-Shifted Interleaving",
          "problem": "An array arr of n positive integers and an unknown positive integer k were used to build an array b of length 2n: for every arr[i], both arr[i]-k and arr[i]+k were placed somewhere into b (in arbitrary order, fully shuffled). Given b, recover any valid array arr and the value k that could have produced it (if multiple answers exist, any valid one is acceptable).\n\nInput: 2n (the length of b), then the 2n integers of b.\nOutput: the n integers of a valid arr, sorted ascending, space-separated (print IMPOSSIBLE if no valid reconstruction exists, which will not occur in this example).",
          "approach": "Sort b. The globally smallest element of b, b[0], must be a 'low' member of some pair (arr[i]-k), since arr[i]+k > arr[i]-k for k > 0, so nothing can be smaller than the smallest low value. Therefore the true k satisfies b[0] + 2k = (partner of b[0]) for SOME other element of b; try every candidate k = (b[i] - b[0]) / 2 for i = 1..2n-1 (skipping non-positive or odd differences). For each candidate k, attempt a greedy two-pointer-style matching using a multiset: repeatedly remove the current smallest remaining value x, and require x + 2k to also be present in the multiset (removing it too) as x's high partner; record x + k as a recovered element of arr. If this succeeds for all 2n elements (the multiset empties with no failed lookups), the candidate k is valid and the recovered elements form arr. Processing smallest-to-largest is essential: it guarantees every value taken as 'x' is genuinely a low member (its partner, if any, still lies ahead in sorted order), so the matching can never mistakenly try to pair two high members together.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n2; cin >> n2; // total length, must be even\n    vector<long long> b(n2);\n    for (auto &x : b) cin >> x;\n    sort(b.begin(), b.end());\n    int n = n2 / 2;\n\n    vector<long long> result;\n    bool found = false;\n    for (int i = 1; i < n2 && !found; i++) {\n        long long diff = b[i] - b[0];\n        if (diff <= 0 || diff % 2 != 0) continue;\n        long long k = diff / 2;\n        // attempt greedy matching with this k using a multiset\n        multiset<long long> ms(b.begin(), b.end());\n        vector<long long> res;\n        bool ok = true;\n        while (!ms.empty()) {\n            long long x = *ms.begin();\n            ms.erase(ms.begin());\n            auto it = ms.find(x + 2*k);\n            if (it == ms.end()) { ok = false; break; }\n            ms.erase(it);\n            res.push_back(x + k);\n        }\n        if (ok && (long long)res.size() == n) {\n            result = res;\n            found = true;\n        }\n    }\n    if (!found) {\n        cout << \"IMPOSSIBLE\" << endl;\n        return 0;\n    }\n    sort(result.begin(), result.end());\n    for (size_t i = 0; i < result.size(); i++) cout << result[i] << (i+1<result.size()?' ':'\\n');\n}\n",
          "sample_input": "6\n33 7 23 13 27 17\n",
          "expected_output": "10 20 30\n"
        }
      ]
    },
    {
      "name": "Monotonic Deque",
      "theory": "A monotonic deque maintains a double-ended queue of candidate values (or indices) that is always sorted (monotonically increasing or decreasing) from front to back, and supports O(1) amortized insertion at the back and removal from either end. It is the standard tool for computing a sliding window's maximum or minimum in O(n) total, and more generally for online DP transitions of the form dp[i] = f(i) + best(dp[j] for j in a moving window [i-k, i-1]).\n\nFor sliding window maximum, the deque stores indices with strictly decreasing values front-to-back. When a new index i arrives: (1) pop from the back any index whose value is <= a[i], because such an index can never again be the window maximum while i is still in the window (a[i] is at least as large and will outlast it) -- this is what keeps the deque monotonic and is the reason the amortized cost is O(1) (each element is pushed once and popped at most once); (2) push i to the back; (3) pop from the front any index that has fallen outside the current window's left boundary; (4) the current window's maximum is now the value at the front of the deque.\n\nA second major use case is when the naive two-pointer sliding window technique breaks because the array contains negative numbers, so prefix sums are not monotonic. For 'shortest subarray with sum >= K', a two-pointer approach that only ever advances both pointers forward is unsound because shrinking the window from the left doesn't necessarily decrease the sum. The fix is to work with prefix sums pre[0..n] and maintain a deque of indices with strictly INCREASING prefix-sum values front-to-back: for each new index j, first pop from the FRONT any index i where pre[j] - pre[i] >= K (recording the window length j - i as a candidate answer, and popping i because it can never again be useful -- any later j' > j gives an even longer, so worse, candidate through the same i); then pop from the BACK any index i where pre[i] >= pre[j] (such an i is dominated by j: j is a later index with an equal or smaller prefix sum, so j is always at least as good a left endpoint for future queries); finally push j to the back. Both directions of pruning together guarantee the deque never grows unboundedly and every index is pushed and popped at most once, giving O(n) total time.\n\nMonotonic-deque DP optimization follows the same shape applied to a DP recurrence instead of a raw array: whenever the transition looks at a max (or min) of dp-values over a sliding window of previous states, replacing the naive O(window size) scan with a monotonic deque turns an O(n*k) DP into O(n).",
      "recognition": "You need the max or min of every sliding window of an array (fixed or variable size) computed as the window moves, ideally in a single pass; or a DP transition needs 'the best dp-value among the last k states' computed online; or a two-pointer sliding-window sum breaks because the array contains negative numbers, requiring a monotonic structure on prefix sums instead.",
      "complexity": "O(n) time and O(k) or O(n) space (amortized O(1) per element, since each index is pushed and popped from the deque at most once).",
      "examples": [
        {
          "title": "Sliding Window Maximum",
          "problem": "Given an array nums of n integers and a window size k, return the maximum value of every contiguous window of size k as the window slides from the left end of the array to the right end (there are n - k + 1 such windows).\n\nInput: n and k, then n integers nums[1..n].\nOutput: the n - k + 1 window maximums, space-separated.",
          "approach": "Maintain a deque of indices whose corresponding values are strictly decreasing from front to back. For each index i from 0 to n-1: pop indices from the back of the deque while the value at the back is <= nums[i] (they can never be the max again once nums[i] is in play); push i to the back. Pop from the front any index that is no longer within the window (index <= i - k). Once i >= k - 1 (the window is full), the front of the deque holds the index of the current window's maximum, so record nums[front]. Each index enters and leaves the deque at most once, giving O(n) total time.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n, k;\n    cin >> n >> k;\n    vector<int> a(n);\n    for (auto &x : a) cin >> x;\n    deque<int> dq; // stores indices, values decreasing front-to-back\n    vector<int> res;\n    for (int i = 0; i < n; i++) {\n        while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back();\n        dq.push_back(i);\n        while (dq.front() <= i - k) dq.pop_front();\n        if (i >= k - 1) res.push_back(a[dq.front()]);\n    }\n    for (size_t i = 0; i < res.size(); i++) cout << res[i] << (i+1<res.size()?' ':'\\n');\n}\n",
          "sample_input": "8 3\n1 3 -1 -3 5 3 6 7\n",
          "expected_output": "3 3 5 5 6 7\n"
        },
        {
          "title": "Shortest Subarray with Sum at Least K",
          "problem": "Given an integer array nums of n elements (which may include negative numbers) and an integer K, return the length of the shortest, non-empty, contiguous subarray whose sum is at least K. If no such subarray exists, return -1.\n\nInput: n and K, then n integers nums[1..n].\nOutput: the length of the shortest qualifying subarray, or -1.",
          "approach": "Because nums can contain negative numbers, prefix sums pre[0..n] are not monotonic, so a standard two-pointer sliding window (which relies on the sum only increasing as the right end grows and only decreasing as the left end shrinks) is unsound here. Instead, maintain a deque of indices into pre[] with strictly increasing pre[] values front-to-back. For each j from 0 to n: while the deque is non-empty and pre[j] - pre[front] >= K, that gives a candidate answer of length j - front; record it and pop the front (a smaller index can never yield a shorter valid subarray again once a later, equally-or-more-restrictive j has already worked for it -- any future j' > j would only produce a longer candidate through the same front). Then, while the deque's back has pre[] >= pre[j], pop it (index j is a strictly better or equal left endpoint for all future right endpoints, since it has an equal-or-smaller prefix sum at a later, closer position). Push j to the back. The answer is the minimum recorded length, or -1 if none was found.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; long long K;\n    cin >> n >> K;\n    vector<long long> a(n);\n    for (auto &x : a) cin >> x;\n    vector<long long> pre(n+1, 0);\n    for (int i = 0; i < n; i++) pre[i+1] = pre[i] + a[i];\n    deque<int> dq; // indices into pre[], increasing prefix-sum order front-to-back\n    int best = INT_MAX;\n    for (int j = 0; j <= n; j++) {\n        while (!dq.empty() && pre[j] - pre[dq.front()] >= K) {\n            best = min(best, j - dq.front());\n            dq.pop_front();\n        }\n        while (!dq.empty() && pre[dq.back()] >= pre[j]) dq.pop_back();\n        dq.push_back(j);\n    }\n    cout << (best == INT_MAX ? -1 : best) << endl;\n}\n",
          "sample_input": "3 3\n2 -1 2\n",
          "expected_output": "3\n"
        }
      ]
    },
    {
      "name": "Merge Intervals",
      "theory": "Merge Intervals problems build on the Sorting & Binary Search idea that sorting objects by a key transforms an unordered comparison problem into a linear sweep. Here the objects are ranges [l, r] rather than points, and the standard first step is to sort all intervals by their left endpoint. Once sorted, a single left-to-right pass can merge overlapping or touching intervals: keep a running 'current merged interval'; for each new interval in sorted order, if its start is <= the current merged interval's end, extend the current interval's end to the max of the two ends (they overlap or touch, so they belong to the same merged group); otherwise, the current merged interval is finalized (push it to the result) and the new interval starts a fresh current interval. This is correct because sorting by start guarantees that once an interval's start exceeds the current merged end, no LATER interval (which has an even larger or equal start) can ever overlap the current merged interval either.\n\nMany harder problems combine this sort-and-sweep idea with a secondary data structure to answer aggregate or per-query questions rather than just producing the merged list. A common pattern: sort both the intervals (by start) and a separate list of queries, then sweep through the queries in increasing order, incrementally adding to a min-heap (or other structure) every interval whose start has already been 'reached' by the sweep, lazily removing from the heap any interval that has expired (its end is now behind the current query point). This 'offline sweep with a heap keyed by a secondary attribute' technique answers many queries in O((n + q) log n) total instead of O(n) per query.\n\nA third pattern extends interval merging into a full mutable data structure: rather than merging a static list once, the structure supports online insertions and/or deletions of ranges, automatically re-merging with neighbors on every update (e.g. a TreeMap/ordered-map of disjoint intervals keyed by start, where inserting a new point or range requires finding and splicing together any now-adjacent or overlapping neighboring intervals). This turns 'merge intervals' from a one-shot batch algorithm into an amortized-efficient online interval-set structure.",
      "recognition": "You are given a list of ranges/intervals and need to combine overlapping ones into a canonical disjoint form, or you need to answer 'which merged range covers this point/query' efficiently, possibly across many queries or many incremental updates to the interval set.",
      "complexity": "O(n log n) time to sort and merge n intervals once; O((n + q) log n) for an offline sweep answering q queries against n intervals with a heap.",
      "examples": [
        {
          "title": "Merge Overlapping Intervals",
          "problem": "Given a collection of n intervals, each represented as [start, end] (inclusive), merge all overlapping (or touching) intervals and return the resulting list of disjoint intervals, sorted by start.\n\nInput: n, then n pairs of integers start end.\nOutput: the number of merged intervals, followed by each merged interval on its own line as 'start end'.",
          "approach": "Sort the intervals by start. Initialize the result with the first interval as the 'current' interval. For each subsequent interval in sorted order, compare its start to the current interval's end: if start <= current end, the intervals overlap or touch, so extend current's end to max(current end, this interval's end); otherwise, push the current interval to the result and make this interval the new current. After the loop, push the final current interval. This is a single O(n) pass after an O(n log n) sort.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; cin >> n;\n    vector<pair<long long,long long>> iv(n);\n    for (auto &p : iv) cin >> p.first >> p.second;\n    sort(iv.begin(), iv.end());\n    vector<pair<long long,long long>> res;\n    for (auto &p : iv) {\n        if (!res.empty() && p.first <= res.back().second) {\n            res.back().second = max(res.back().second, p.second);\n        } else {\n            res.push_back(p);\n        }\n    }\n    cout << res.size() << \"\\n\";\n    for (auto &p : res) cout << p.first << \" \" << p.second << \"\\n\";\n}\n",
          "sample_input": "4\n1 3\n2 6\n8 10\n15 18\n",
          "expected_output": "3\n1 6\n8 10\n15 18\n"
        },
        {
          "title": "Minimum Interval to Include Each Query",
          "problem": "You are given n intervals, each [start, end] (inclusive), and q query points. For each query point x, find the size (end - start + 1) of the smallest interval that contains x. If no interval contains x, the answer for that query is -1.\n\nInput: n, then n pairs start end, then q, then q query integers.\nOutput: q integers (one per query, in the original query order), space-separated, where an unanswerable query prints -1.",
          "approach": "Sort intervals by start, and sort the queries by value (while remembering each query's original index so the output can be restored to input order). Sweep the sorted queries left to right: maintain a pointer into the sorted intervals and a min-heap keyed by (interval size, interval end). For each query x, first push into the heap every interval whose start is <= x that has not yet been pushed (advancing the interval pointer). Then pop from the heap any interval whose end < x (it has expired and can never contain this or any larger future query point). The top of the heap, if any remains, has the smallest size among all intervals currently containing x, which is exactly the answer for this query. Because both intervals and queries are processed in sorted order with a heap, each interval is pushed and popped at most once, giving O((n + q) log n) total time.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; cin >> n;\n    vector<pair<long long,long long>> iv(n); // start, end (inclusive)\n    for (auto &p : iv) cin >> p.first >> p.second;\n    int q; cin >> q;\n    vector<long long> queries(q);\n    vector<int> order(q);\n    for (int i = 0; i < q; i++) { cin >> queries[i]; order[i] = i; }\n\n    sort(iv.begin(), iv.end()); // by start\n    sort(order.begin(), order.end(), [&](int i, int j){ return queries[i] < queries[j]; });\n\n    // min-heap of (size, end)\n    priority_queue<pair<long long,long long>, vector<pair<long long,long long>>, greater<>> pq;\n    vector<long long> ans(q, -1);\n    int idx = 0;\n    for (int oi : order) {\n        long long x = queries[oi];\n        while (idx < n && iv[idx].first <= x) {\n            pq.push({iv[idx].second - iv[idx].first + 1, iv[idx].second});\n            idx++;\n        }\n        while (!pq.empty() && pq.top().second < x) pq.pop();\n        if (!pq.empty()) ans[oi] = pq.top().first;\n    }\n    for (int i = 0; i < q; i++) cout << ans[i] << (i+1<q?' ':'\\n');\n}\n",
          "sample_input": "4\n1 4\n2 4\n3 6\n4 4\n4\n2 3 4 5\n",
          "expected_output": "3 3 1 4\n"
        }
      ]
    }
  ]
}