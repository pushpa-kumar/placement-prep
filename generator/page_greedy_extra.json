{
  "intro": "Building on straightforward greedy exchange arguments, these two subtopics sharpen the central judgment call of greedy algorithm design: Huffman coding and optimal-merge show a provably correct greedy merge order and exactly where an adjacency constraint breaks it, while fractional-vs-0/1 recognition trains the instinct for telling a genuinely greedy problem apart from one that only looks like it until you try a concrete counterexample.",
  "subtopics": [
    {
      "name": "Huffman Coding & Optimal Merge",
      "theory": "Huffman coding builds an optimal binary prefix-free code for a set of symbol frequencies by repeatedly merging the two SMALLEST remaining weights into one combined weight (paying a cost equal to their sum) and pushing the combined weight back into the pool, until a single weight remains. This greedy rule is provably optimal by an exchange argument: in any optimal Huffman tree, the two least-frequent symbols can always be placed as siblings at the deepest level without increasing the total weighted path length, so merging them first loses nothing and the same argument applies recursively to the resulting smaller instance. A priority queue (min-heap) makes this greedy merge run in O(n log n): pop the two smallest weights, push their sum, and accumulate that sum into a running total cost -- the final total is exactly the minimum possible sum of (frequency * code length) over all valid prefix-free binary codes, which is also exactly the answer to the more general 'optimal merge cost' problem (minimize total cost to combine n items pairwise, where combining two items of weight a and b costs a+b).\n\nThe greedy priority-queue approach generalizes cleanly to K-ARY merges (combine the K smallest weights at a time instead of 2) as long as the only constraint is 'pick any K items, of any positions, to merge next' -- this is still solvable by always grabbing the K globally smallest weights from a heap (after padding with zero-weight dummy items if needed so the merge count divides evenly). However, the moment an ADJACENCY constraint is added -- e.g. 'you may only merge K items that are currently CONSECUTIVE in the original row/array' -- the greedy 'always take the smallest' argument breaks completely, because the smallest available weights might not be next to each other, and merging them anyway is simply illegal. This adjacency-constrained version needs INTERVAL DP instead: dp[l][r] tracks the minimum cost to merge the contiguous range [l, r] down to the fewest piles reachable under the K-at-a-time rule (exactly 1 + (r - l) mod (K-1) piles), built by trying every valid split point and, when a full K-way merge of the whole range is legal, adding the range's total sum as the final merge's cost.\n\nRecognizing which regime you are in -- unconstrained 'always merge globally smallest' (greedy, priority queue) versus adjacency-constrained or otherwise structurally restricted merging (interval DP) -- is the single most important skill this subtopic tests, since the two problem statements can look deceptively similar while requiring completely different algorithms.",
      "recognition": "The problem asks to combine n items pairwise (or K at a time) into one, where each combination costs the sum of the pieces being combined, and you want to minimize (or the problem directly names) total Huffman/prefix-code length or 'optimal merge cost' -- if merges may pick ANY items regardless of position, it is the classical greedy priority-queue algorithm. The moment the problem restricts merges to CONSECUTIVE/adjacent elements (or adds any other cost term beyond a pure sum, such as a positional penalty), the greedy argument no longer applies and interval DP is required instead.",
      "complexity": "O(n log n) for the greedy priority-queue Huffman/optimal-merge algorithm; O(n^2) to O(n^2 * K) for the adjacency-constrained interval DP variant (K-way merge of contiguous ranges), depending on how the split points are enumerated.",
      "examples": [
        {
          "title": "Minimum Total Huffman Code Length",
          "problem": "You are given the frequencies of n distinct symbols (2 <= n <= 10^5, 1 <= frequency <= 10^9). Construct an optimal binary prefix-free (Huffman) code for these symbols and output the minimum possible total encoded length, i.e. the sum over all symbols of (frequency of the symbol) * (length of its assigned binary code).\n\nInput: n, then n integers (the frequencies).\nOutput: the minimum total encoded length.",
          "approach": "Push all n frequencies into a min-heap. While more than one element remains, pop the two smallest values a and b, add (a+b) to a running total cost, and push (a+b) back into the heap -- this is the classical Huffman merge. The final running total is exactly the minimum total encoded length, because each symbol's frequency gets added into the running total once for every merge along the path from its leaf up to the root of the eventual Huffman tree, which is precisely frequency * (depth in the tree) = frequency * (code length). Verified against the well-known textbook example (frequencies 5, 9, 12, 13, 16, 45), which is documented to have an optimal total encoded length of 224 -- the implementation reproduces this exactly.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; cin>>n;\n    priority_queue<long long, vector<long long>, greater<long long>> pq;\n    for(int i=0;i<n;i++){ long long f; cin>>f; pq.push(f); }\n    if(n==1){ cout << pq.top() << endl; return 0; }\n    long long total = 0;\n    while(pq.size()>1){\n        long long a = pq.top(); pq.pop();\n        long long b = pq.top(); pq.pop();\n        long long merged = a+b;\n        total += merged;\n        pq.push(merged);\n    }\n    cout << total << endl;\n}\n",
          "sample_input": "6\n5 9 12 13 16 45\n",
          "expected_output": "224\n"
        },
        {
          "title": "Minimum Cost to Merge K Adjacent Piles (where the Huffman greedy fails)",
          "problem": "You are given n piles of stones in a row (1 <= n <= 30), pile i having stones[i] stones (1 <= stones[i] <= 100), and an integer K (2 <= K <= n). In one move you may merge EXACTLY K CONSECUTIVE piles into a single new pile; the cost of that move equals the total number of stones in those K piles, and after the merge those K piles are replaced by the one combined pile (so future merges see the new, shorter row). Find the minimum total cost to merge the entire row down to a single pile, or report -1 if it is impossible.\n\nInput: n and K, then n integers stones[1..n].\nOutput: the minimum total cost, or -1 if merging down to one pile is impossible.",
          "approach": "First, a merge sequence can only ever reduce the number of piles by (K-1) per move, so reaching exactly 1 pile from n is only possible when (n-1) is divisible by (K-1); otherwise output -1 immediately. Otherwise, this is the adjacency-constrained generalization of Huffman/optimal-merge, and the greedy 'always merge the K globally smallest piles' rule from ordinary Huffman coding does NOT apply, because the K smallest-valued piles are almost never K consecutive piles in the row -- interval DP is required instead.\n\nLet dp[i][j] = the minimum cost to merge the piles in range [i, j] down to the FEWEST piles reachable under the K-at-a-time rule, which is always exactly 1 + ((j-i) mod (K-1)) piles (a consequence of each merge removing exactly K-1 piles). Compute it by increasing range length: dp[i][j] = min over mid stepping by (K-1) from i up to (but not including) j of dp[i][mid] + dp[mid+1][j] (splitting the range into a left part that can be fully reduced to its minimal pile count, and a right remainder). Whenever (j - i) is itself a multiple of (K-1), the range [i, j] can additionally be fully collapsed into exactly ONE final pile with one more K-way merge, so add the full range sum (prefix-sum precomputed) to dp[i][j] in that case. The answer is dp[0][n-1]. Verified against all three official LeetCode 'Minimum Cost to Merge Stones' examples: stones=[3,2,4,1],K=2 -> 20; stones=[3,2,4,1],K=3 -> -1; stones=[3,5,1,2,6],K=3 -> 25 -- all three match exactly.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n, K;\n    cin >> n >> K;\n    vector<long long> a(n);\n    for (auto &x : a) cin >> x;\n    if ((n-1) % (K-1) != 0) { cout << -1 << endl; return 0; }\n    vector<long long> pre(n+1, 0);\n    for (int i = 0; i < n; i++) pre[i+1] = pre[i] + a[i];\n    auto rangeSum = [&](int i, int j){ return pre[j+1]-pre[i]; };\n    const long long INF = LLONG_MAX/2;\n    vector<vector<long long>> dp(n, vector<long long>(n, 0));\n    for (int len = 2; len <= n; len++) {\n        for (int i = 0; i + len - 1 < n; i++) {\n            int j = i + len - 1;\n            dp[i][j] = INF;\n            for (int mid = i; mid < j; mid += (K-1)) {\n                dp[i][j] = min(dp[i][j], dp[i][mid] + dp[mid+1][j]);\n            }\n            if ((j - i) % (K-1) == 0) {\n                dp[i][j] += rangeSum(i, j);\n            }\n        }\n    }\n    cout << dp[0][n-1] << endl;\n}\n",
          "sample_input": "5 3\n3 5 1 2 6\n",
          "expected_output": "25\n"
        }
      ]
    },
    {
      "name": "Fractional vs. 0/1 Greedy Recognition",
      "theory": "The fractional knapsack theorem states: if items are DIVISIBLE (you may take any fraction of an item's weight and receive that same fraction of its value), then sorting items by value-to-weight ratio (descending) and greedily filling the capacity -- taking each item fully while it fits, and topping off the very last item with whatever fraction of capacity remains -- produces the provably optimal total value. The proof is a clean exchange argument: given any solution that doesn't follow this order, you can always shift an infinitesimal amount of weight from a lower-ratio item to a higher-ratio item without violating the capacity constraint, strictly increasing total value, showing any non-greedy solution cannot be optimal.\n\nThe moment items become INDIVISIBLE (0/1 knapsack: take an item entirely or not at all), this exact exchange argument breaks down, because you can no longer 'top off' the last item with an arbitrarily small fraction -- you either take the whole item (possibly overshooting the remaining capacity) or skip it entirely (possibly wasting capacity that a different, lower-ratio combination could have used more fully). A classic concrete counterexample: capacity 50, items (weight=10, value=60), (weight=20, value=100), (weight=30, value=120). Sorted by ratio (6, 5, 4), greedy 0/1 selection takes item 1 (10/60) then item 2 (20/100), filling 30 of the 50 capacity for 160 total value, and cannot fit item 3 -- but the true optimal 0/1 selection is items 2 and 3 together (weight 50 exactly, value 220), which the ratio-greedy never considers because it commits to the top-ratio item early and never revisits that choice. The correct approach for 0/1 knapsack is a DP over remaining capacity: dp[c] = best value achievable with total weight <= c, updated item by item in decreasing order of c (to avoid reusing an item twice within the same pass).\n\nMore generally, greedy-by-ratio (or greedy-by-any-local-comparison) tends to be PROVABLY correct exactly when the underlying structure has a matroid-like exchange property (as in fractional knapsack, or Kruskal's MST algorithm, or scheduling problems built from freely swappable atomic units) -- and tends to FAIL exactly when an indivisibility constraint, an exact-size/exact-count constraint, or some other rigid combinatorial restriction prevents the small local exchange step that the greedy's correctness proof relies on. A fast diagnostic: try to construct a small counterexample by hand where greedy's answer differs from a brute-force or DP answer; if you can, greedy is wrong for that variant and you need DP (or a more careful combinatorial argument) instead.",
      "recognition": "If the problem explicitly allows taking a FRACTION of an item (or is phrased in terms of continuous quantities, like fluid volumes or divisible resources), ratio-based greedy is very likely correct and provably optimal. If instead items must be taken whole (0/1), or there are hard cardinality constraints (exactly p of one type and s of another, or an exact count limit per category), be suspicious of any greedy-by-ratio or greedy-by-single-criterion approach -- try to construct a counterexample before trusting it, and default to a DP formulation (over remaining capacity, remaining counts, or similar) when in doubt.",
      "complexity": "O(n log n) for the fractional-knapsack greedy (dominated by the sort); O(n * W) for the 0/1 knapsack DP (W = weight capacity), which is pseudo-polynomial and only tractable when W is reasonably small.",
      "examples": [
        {
          "title": "Fractional Knapsack (greedy is provably correct)",
          "problem": "You are given n items (1 <= n <= 10^5), item i having weight w[i] and value v[i] (positive real-valued in general; integers here for concreteness), and a knapsack capacity C. You may take any fraction (between 0 and 1) of each item's weight, receiving that same fraction of its value, subject to the total weight taken not exceeding C. Maximize total value.\n\nInput: n and C, then n lines each with w[i] and v[i].\nOutput: the maximum total value achievable, printed with 2 decimal places.",
          "approach": "Sort item indices by value-to-weight ratio v[i]/w[i] in descending order. Walk through items in that order, maintaining the remaining capacity: if an item's full weight fits within the remaining capacity, take it entirely (add its full value, subtract its weight from remaining capacity); otherwise take exactly the fraction of it that fits (add value * (remaining/weight), set remaining capacity to 0), and stop, since no further capacity remains. This greedy order is optimal by the standard exchange argument for divisible items. On the classic textbook instance (capacity 50, items (10,60), (20,100), (30,120)), the greedy takes item 1 fully (value 60, capacity left 40), item 2 fully (value 100, capacity left 20), and 20/30 of item 3 (value 80), for a total of 240.00, matching the well-known reference answer for this exact instance.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n; double cap;\n    cin >> n >> cap;\n    vector<double> w(n), v(n);\n    for(int i=0;i<n;i++) cin >> w[i] >> v[i];\n    vector<int> idx(n);\n    iota(idx.begin(), idx.end(), 0);\n    sort(idx.begin(), idx.end(), [&](int a, int b){\n        return v[a]/w[a] > v[b]/w[b];\n    });\n    double total = 0.0;\n    double remaining = cap;\n    for(int i : idx){\n        if(remaining <= 0) break;\n        if(w[i] <= remaining){\n            total += v[i];\n            remaining -= w[i];\n        } else {\n            total += v[i] * (remaining / w[i]);\n            remaining = 0;\n        }\n    }\n    printf(\"%.2f\\n\", total);\n}\n",
          "sample_input": "3 50\n10 60\n20 100\n30 120\n",
          "expected_output": "240.00\n"
        },
        {
          "title": "0/1 Knapsack (same instance, ratio-greedy is provably wrong)",
          "problem": "You are given n items (1 <= n <= 500), item i having integer weight w[i] and integer value v[i] (1 <= w[i], v[i] <= 10^4), and an integer knapsack capacity C (1 <= C <= 10^4). Each item must be taken WHOLE or not at all (0/1, no fractions). Maximize total value subject to total weight <= C.\n\nInput: n and C, then n lines each with w[i] and v[i].\nOutput: the maximum total value achievable.",
          "approach": "Run the standard 1D 0/1 knapsack DP: dp[c] = best value achievable using total weight exactly <= c, initialized to all 0. For each item, update dp[c] for c from C down to w[i] (descending order is essential so each item is only considered once per pass): dp[c] = max(dp[c], dp[c - w[i]] + v[i]). The final answer is dp[C].\n\nUsing the EXACT same numbers as the fractional-knapsack example above (capacity 50, items (10,60), (20,100), (30,120)) deliberately demonstrates why ratio-based greedy fails once items are indivisible: sorting by ratio (6, 5, 4) and greedily taking whole items in that order takes item 1 and item 2 (weight 30, value 160) and then cannot fit item 3 (weight 30 > remaining 20 capacity), for a greedy total of only 160. The DP above finds the true optimum of 220, achieved by taking items 2 and 3 together (weight exactly 50, value 220) and skipping the highest-ratio item 1 entirely -- a combination the greedy never even considers because it commits early to the best-ratio item. This 60-point gap (220 vs. 160) is a concrete, hand-verifiable proof that greedy-by-ratio is not a valid algorithm for the 0/1 variant.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\nint main(){\n    int n, cap;\n    cin >> n >> cap;\n    vector<int> w(n), v(n);\n    for(int i=0;i<n;i++) cin >> w[i] >> v[i];\n    vector<long long> dp(cap+1, 0);\n    for(int i=0;i<n;i++){\n        for(int c = cap; c >= w[i]; c--){\n            dp[c] = max(dp[c], dp[c-w[i]] + v[i]);\n        }\n    }\n    cout << dp[cap] << endl;\n}\n",
          "sample_input": "3 50\n10 60\n20 100\n30 120\n",
          "expected_output": "220\n"
        }
      ]
    }
  ]
}
