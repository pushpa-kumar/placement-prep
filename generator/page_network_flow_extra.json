{
  "intro": "Once max-flow is established, attaching a per-unit cost to each edge (min-cost max-flow) or specializing to complete bipartite matching with a cost matrix (the Hungarian algorithm) unlocks a large family of assignment and scheduling problems that plain max-flow cannot express.",
  "subtopics": [
    {
      "name": "Min-Cost Max-Flow (MCMF)",
      "theory": "Min-cost max-flow generalizes ordinary max-flow by giving every edge a per-unit COST in addition to its capacity, and asking for the maximum flow that also has the smallest possible total cost (sum over edges of flow(e) * cost(e)), or, in a common variant, the smallest cost to push some REQUIRED amount of flow (not necessarily the maximum). The standard algorithm is successive shortest augmenting paths (SSP): repeatedly find the CHEAPEST path from source to sink in the residual graph (using shortest-path search, since a cheapest path is exactly a shortest path when edge weights are the costs), push as much flow as the path's bottleneck capacity allows, and repeat until either no augmenting path remains (max-flow case) or the required flow amount has been shipped (required-flow case). Crucially, when you push flow along an edge, you must add a REVERSE residual edge with the NEGATED cost (mirroring how max-flow adds reverse edges with negated... well, zero-cost residual capacity, but here also negative cost) — this reverse edge lets a later augmenting path 'undo' part of an earlier, now-suboptimal, flow assignment, which is essential for overall optimality. Because of these negative-cost reverse edges, Dijkstra cannot be used directly on the raw graph; the two standard fixes are (a) SPFA / Bellman-Ford-with-a-queue, which tolerates negative edges directly at the cost of a worse worst-case complexity, or (b) Johnson's-algorithm-style potentials, which maintain a running set of vertex potentials that make all reduced edge costs non-negative, allowing Dijkstra (with a priority queue) after the very first SPFA/Bellman-Ford pass — the potentials approach is asymptotically much faster on graphs with many augmenting iterations and is the standard choice in production MCMF libraries, though a straightforward SPFA-based implementation is simpler to get right under contest time pressure and is what is shown below.\n\nA critical correctness property of SSP is that shortest-path DISTANCES to the sink are non-decreasing across successive iterations, as long as the very first iteration's shortest paths are computed correctly (this is what justifies using Dijkstra-with-potentials after the first Bellman-Ford pass, and it is also why greedily always taking the cheapest available path, one at a time, produces a GLOBALLY minimum-cost solution rather than merely a locally greedy one — this is the deep reason SSP is provably optimal and not just a heuristic).\n\nThe hard part of most MCMF problems in practice is not the algorithm itself (a well-tested MCMF template is typically copy-pasted) but recognizing and constructing the FLOW NETWORK from a combinatorial problem statement that does not superficially mention flow at all: assignment problems where the same unit can be routed through different 'paths' at different costs, budgeted resource allocation with per-source or per-destination caps (modeled as edge capacities) and per-unit prices (modeled as edge costs), and coloring/labeling problems with per-vertex balance constraints (modeled by splitting each vertex into an in-node and out-node connected by a capacity-and-cost-carrying edge, the classic 'node splitting' trick). Recognizing when a problem is really an assignment/transportation problem in disguise, and correctly setting up capacities (which encode 'how much', e.g. a budget or a supply limit) versus costs (which encode 'how expensive per unit', e.g. a price or a penalty) is the single most important skill this subtopic tests.",
      "recognition": "The problem involves assigning, routing, or matching UNITS of something (goods, characters, tasks, time slots) through multiple possible channels/pairings, each channel has both a CAPACITY (how much can flow through it) and a PER-UNIT COST, and you need either (a) the minimum cost to achieve the maximum possible throughput, or (b) the minimum cost to achieve some specific REQUIRED throughput (with -1 or 'impossible' if that throughput cannot be reached at all).",
      "complexity": "O(F * E log V) with Dijkstra + potentials, or O(F * V * E) with plain SPFA/Bellman-Ford augmenting paths, where F is the total flow pushed (bounded by the max-flow value or the required flow amount), V and E are the network's vertex and edge counts.",
      "examples": [
        {
          "title": "Maximum Flow at Minimum Cost on a Small Network",
          "problem": "You are given a directed flow network with n nodes (0-indexed), a designated source s and sink t, and m directed edges, each with a capacity and a per-unit cost. Compute the value of the maximum flow from s to t, and, among all flows achieving that maximum value, the minimum possible total cost.\n\nInput: n, m, s, t on the first line; then m lines each with u, v, cap, cost describing a directed edge from u to v.\nOutput: two integers, the maximum flow value and its minimum total cost, separated by a space.",
          "approach": "Build the flow network with a standard adjacency-list MCMF structure: each added edge also creates a reverse residual edge with capacity 0 and NEGATED cost. Repeatedly run SPFA (Bellman-Ford with a FIFO queue, needed because reverse residual edges carry negative costs) from s to find the CHEAPEST path to every reachable node; if the sink is unreachable, stop. Otherwise, find the bottleneck capacity along the cheapest s-to-t path (the minimum residual capacity of any edge on that path), push that much flow along it (decreasing forward capacities and increasing the corresponding reverse capacities), and add (bottleneck * path cost) to the running total cost. Repeat until no augmenting path remains; the accumulated flow is the max-flow value and the accumulated cost is the minimum cost for that flow.\n\nThis was hand-verified on the given 4-node network: source 0, sink 3, edges 0->1 (cap 2, cost 2), 0->2 (cap 1, cost 3), 1->3 (cap 1, cost 1), 1->2 (cap 1, cost 1), 2->3 (cap 2, cost 1). The bottleneck at every source/sink edge forces a unique maximum flow of exactly 3 units that saturates EVERY edge in the network simultaneously (0->1 fully used at 2, 0->2 fully used at 1, 1->3 fully used at 1, 1->2 fully used at 1, 2->3 fully used at 2) — since this is the only possible flow of value 3, its cost is forced: 2*2 + 1*3 + 1*1 + 1*1 + 2*1 = 4+3+1+1+2 = 11, matching the program's output exactly.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\ntypedef long long ll;\nconst ll INF = LLONG_MAX / 2;\n\nstruct MCMF {\n    struct Edge { int to; ll cap, cost; int rev; };\n    int n;\n    vector<vector<Edge>> graph;\n    MCMF(int n) : n(n), graph(n) {}\n    void addEdge(int from, int to, ll cap, ll cost) {\n        graph[from].push_back({to, cap, cost, (int)graph[to].size()});\n        graph[to].push_back({from, 0, -cost, (int)graph[from].size()-1});\n    }\n    // returns {maxflow, mincost} using successive shortest augmenting paths (SPFA, handles negative edges)\n    pair<ll,ll> run(int s, int t) {\n        ll totalFlow = 0, totalCost = 0;\n        while (true) {\n            vector<ll> dist(n, INF);\n            vector<int> prevv(n, -1), preve(n, -1);\n            vector<bool> inq(n, false);\n            dist[s] = 0;\n            deque<int> q;\n            q.push_back(s);\n            inq[s] = true;\n            while (!q.empty()) {\n                int u = q.front(); q.pop_front();\n                inq[u] = false;\n                for (int i = 0; i < (int)graph[u].size(); i++) {\n                    Edge &e = graph[u][i];\n                    if (e.cap > 0 && dist[u] + e.cost < dist[e.to]) {\n                        dist[e.to] = dist[u] + e.cost;\n                        prevv[e.to] = u;\n                        preve[e.to] = i;\n                        if (!inq[e.to]) {\n                            inq[e.to] = true;\n                            q.push_back(e.to);\n                        }\n                    }\n                }\n            }\n            if (dist[t] == INF) break; // no more augmenting paths\n            // find bottleneck capacity along the path\n            ll d = INF;\n            for (int v = t; v != s; v = prevv[v]) {\n                d = min(d, graph[prevv[v]][preve[v]].cap);\n            }\n            for (int v = t; v != s; v = prevv[v]) {\n                graph[prevv[v]][preve[v]].cap -= d;\n                Edge &back = graph[v][graph[prevv[v]][preve[v]].rev];\n                back.cap += d;\n            }\n            totalFlow += d;\n            totalCost += d * dist[t];\n        }\n        return {totalFlow, totalCost};\n    }\n};\n\nint main(){\n    int n, m, s, t;\n    cin >> n >> m >> s >> t;\n    MCMF mcmf(n);\n    for (int i = 0; i < m; i++) {\n        int u, v; ll cap, cost;\n        cin >> u >> v >> cap >> cost;\n        mcmf.addEdge(u, v, cap, cost);\n    }\n    auto [flow, cost] = mcmf.run(s, t);\n    cout << flow << \" \" << cost << endl;\n}\n",
          "sample_input": "4 5 0 3\n0 1 2 2\n0 2 1 3\n1 3 1 1\n1 2 1 1\n2 3 2 1\n",
          "expected_output": "3 11\n"
        },
        {
          "title": "Shipping a Required Amount at Minimum Cost (or Determining Impossibility)",
          "problem": "You must ship exactly D units of cargo from source node s to sink node t through a directed flow network with n nodes and m edges (each with a capacity and a per-unit cost), minimizing total shipping cost. If it is impossible to ship D units through the network at all (even ignoring cost), print -1.\n\nInput: n, m, s, t, D on the first line; then m lines each with u, v, cap, cost.\nOutput: the minimum cost to ship exactly D units, or -1 if D units cannot be shipped.",
          "approach": "This is the 'required flow' variant of MCMF: instead of running successive shortest augmenting paths until NO path remains (which finds the true max-flow), stop as soon as the accumulated flow reaches exactly D, capping the amount pushed along the final augmenting path so the total does not overshoot D. If the loop runs out of augmenting paths (sink becomes unreachable) before reaching D units shipped, D units are infeasible and the answer is -1. Because SSP always selects the CHEAPEST available path first, and cheapest-path costs are non-decreasing across iterations, capping the flow at D automatically yields the minimum cost for shipping exactly D units, not just A valid way to ship D units.\n\nVerified on a 5-node network (source 0, sink 4) with edges 0->1 (cap 4, cost 2), 0->2 (cap 2, cost 2), 1->3 (cap 2, cost 1), 1->4 (cap 3, cost 6), 2->3 (cap 2, cost 1), 3->4 (cap 4, cost 2): the two cheapest paths, 0-1-3-4 (cost 2+1+2=5 per unit, capacity-limited to 2 units by edge 1->3) and 0-2-3-4 (cost 2+1+2=5 per unit, capacity-limited to 2 units by edge 0->2), together supply exactly the required D=4 units at the shared cheapest rate of 5 per unit, for a total cost of 4*5=20 — matching the program's output. Separately verified that requesting D=8 (exceeding the network's true max-flow of 6) correctly yields -1.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\ntypedef long long ll;\nconst ll INF = LLONG_MAX / 2;\n\nstruct MCMF {\n    struct Edge { int to; ll cap, cost; int rev; };\n    int n;\n    vector<vector<Edge>> graph;\n    MCMF(int n) : n(n), graph(n) {}\n    void addEdge(int from, int to, ll cap, ll cost) {\n        graph[from].push_back({to, cap, cost, (int)graph[to].size()});\n        graph[to].push_back({from, 0, -cost, (int)graph[from].size()-1});\n    }\n    // Ships up to `need` units of flow at minimum cost, one shortest augmenting path at a time.\n    // Returns {flowShipped, cost}. If flowShipped < need, it was impossible to ship `need` units.\n    pair<ll,ll> run(int s, int t, ll need) {\n        ll totalFlow = 0, totalCost = 0;\n        while (totalFlow < need) {\n            vector<ll> dist(n, INF);\n            vector<int> prevv(n, -1), preve(n, -1);\n            vector<bool> inq(n, false);\n            dist[s] = 0;\n            deque<int> q;\n            q.push_back(s);\n            inq[s] = true;\n            while (!q.empty()) {\n                int u = q.front(); q.pop_front();\n                inq[u] = false;\n                for (int i = 0; i < (int)graph[u].size(); i++) {\n                    Edge &e = graph[u][i];\n                    if (e.cap > 0 && dist[u] + e.cost < dist[e.to]) {\n                        dist[e.to] = dist[u] + e.cost;\n                        prevv[e.to] = u;\n                        preve[e.to] = i;\n                        if (!inq[e.to]) {\n                            inq[e.to] = true;\n                            q.push_back(e.to);\n                        }\n                    }\n                }\n            }\n            if (dist[t] == INF) break; // no more augmenting paths at all\n            ll d = need - totalFlow;\n            for (int v = t; v != s; v = prevv[v]) {\n                d = min(d, graph[prevv[v]][preve[v]].cap);\n            }\n            for (int v = t; v != s; v = prevv[v]) {\n                graph[prevv[v]][preve[v]].cap -= d;\n                Edge &back = graph[v][graph[prevv[v]][preve[v]].rev];\n                back.cap += d;\n            }\n            totalFlow += d;\n            totalCost += d * dist[t];\n        }\n        return {totalFlow, totalCost};\n    }\n};\n\nint main(){\n    int n, m, s, t; ll need;\n    cin >> n >> m >> s >> t >> need;\n    MCMF mcmf(n);\n    for (int i = 0; i < m; i++) {\n        int u, v; ll cap, cost;\n        cin >> u >> v >> cap >> cost;\n        mcmf.addEdge(u, v, cap, cost);\n    }\n    auto [flow, cost] = mcmf.run(s, t, need);\n    if (flow < need) cout << -1 << endl;\n    else cout << cost << endl;\n}\n",
          "sample_input": "5 6 0 4 4\n0 1 4 2\n0 2 2 2\n1 3 2 1\n1 4 3 6\n2 3 2 1\n3 4 4 2\n",
          "expected_output": "20\n"
        }
      ]
    },
    {
      "name": "Hungarian Algorithm (Assignment Problem)",
      "theory": "The assignment problem asks: given n 'workers' and n 'jobs' and an n x n cost matrix cost[i][j] (the cost of assigning worker i to job j), find a PERFECT matching (a bijection between workers and jobs, i.e. each worker gets exactly one job and vice versa) that minimizes (or, with negated costs, maximizes) the total cost. This is exactly a special case of min-cost max-flow — build a bipartite network with a source connected to every worker (capacity 1 each), every worker connected to every job with the given cost (capacity 1), and every job connected to the sink (capacity 1 each) — but the Hungarian algorithm (also called the Kuhn-Munkres algorithm, in its modern potential-based form sometimes attributed to Jonker-Volgenant) solves it directly in O(n^3) without any explicit flow-network machinery, which is both asymptotically better than a generic MCMF implementation on this special structure and simpler to reason about once the potential-update invariants are internalized.\n\nThe algorithm maintains a potential u_i for every worker and v_j for every job, satisfying the invariant cost[i][j] - u_i - v_j >= 0 for all i, j (with equality along 'tight' edges that are candidates for the eventual matching), and incrementally grows an augmenting structure one worker at a time: for each new worker, it runs a Dijkstra-like shortest-augmenting-path search over the REDUCED costs (cost[i][j] - u_i - v_j) to find an augmenting path from that worker to some free job through the existing partial matching, updates potentials along the way to maintain the invariant, and then flips the matching along the found path. After all n workers have been processed this way, the resulting perfect matching is provably optimal, and the total minimum cost equals -v_0 (or can simply be read off by summing cost[i][match(i)] over the final matching, which is what most implementations do directly for clarity). To solve a MAXIMIZATION version (maximize total profit rather than minimize total cost), simply negate every entry of the profit matrix and run the same minimization algorithm; the optimal assignment is unchanged and the true maximum profit is the negation of the reported minimum cost.\n\nBeyond the literal 'n workers, n jobs, minimize total cost' framing, the Hungarian algorithm's structure applies whenever a problem reduces to choosing a BIJECTION between two equal-size finite sets to optimize a sum of pairwise costs/profits: assigning distinct completion times to tasks to minimize total tardiness penalty, choosing an order to activate a fixed set of time-dependent financial offers to maximize eventual payoff, or any 'match every item on the left to a distinct item on the right' scenario where the pairwise cost/profit matrix is given explicitly or can be computed in O(n^2) total. When n is large (thousands) and the cost structure has extra exploitable regularity (e.g. costs depending only on |i - j| or on a 1-D coordinate difference), a smarter O(n log n) greedy or DP often exists and outperforms the generic O(n^3) Hungarian algorithm — but the Hungarian algorithm remains the fully general fallback whenever the cost matrix has no such special structure.",
      "recognition": "You must pair up every element of one set with a distinct element of another set of the SAME size (a bijection / perfect matching), an explicit or implicitly-computable cost (or profit) is associated with each possible pairing, and you need the minimum total cost (or maximum total profit) over all such bijections. n is usually small-to-moderate (up to several hundred to ~1000, since the algorithm is O(n^3)).",
      "complexity": "O(n^3) time for an n x n cost matrix, O(n^2) space.",
      "examples": [
        {
          "title": "Minimum-Cost Task Assignment",
          "problem": "There are n employees and n tasks (1 <= n <= 300 in the general version; kept small here for hand verification). You are given an n x n cost matrix where cost[i][j] is the cost of assigning employee i to task j. Assign every employee exactly one distinct task (a perfect matching between employees and tasks) to minimize the total cost. Output the minimum total cost, and one valid assignment (for each employee 1..n, the task assigned to it).\n\nInput: n, then n rows of n integers each (the cost matrix, 1-indexed conceptually).\nOutput: the minimum total cost on the first line; then n integers on the second line, where the i-th integer is the task assigned to employee i.",
          "approach": "Direct application of the O(n^3) Hungarian algorithm with worker/job potentials (u_i, v_j) and per-worker shortest-augmenting-path search over reduced costs, as described in the theory. Each new worker i is temporarily attached to a virtual 'unassigned' job (column 0 in the 1-indexed internal formulation), and the algorithm grows a shortest-path tree from that worker through the existing partial matching until it reaches a genuinely free job, then flips the matching along that path and updates potentials so the non-negativity invariant is preserved for the next worker.\n\nHand-verified on the 3x3 cost matrix [[4,1,3],[2,0,5],[3,2,2]]: brute-forcing all 3! = 6 possible assignments gives costs 6, 11, 5, 9, 10, 6 for the six permutations, so the true minimum is 5, achieved uniquely by employee1->task2 (cost 1), employee2->task1 (cost 2), employee3->task3 (cost 2), summing to 5 — exactly matching the program's output of total cost 5 and assignment (2, 1, 3).",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\ntypedef long long ll;\nconst ll INF = LLONG_MAX / 2;\n\n// Jonker-Volgenant / Kuhn-Munkres O(n^3) Hungarian algorithm for the square\n// assignment problem: minimize sum of cost[i][match(i)] over a perfect matching.\n// 1-indexed internally (standard formulation with potentials u,v).\nint n;\nvector<vector<ll>> cost; // 1-indexed n x n\n\npair<ll, vector<int>> hungarianMin() {\n    vector<ll> u(n+1, 0), v(n+1, 0);\n    vector<int> p(n+1, 0), way(n+1, 0); // p[j] = row assigned to column j\n    for (int i = 1; i <= n; i++) {\n        p[0] = i;\n        int j0 = 0;\n        vector<ll> minv(n+1, INF);\n        vector<bool> used(n+1, false);\n        do {\n            used[j0] = true;\n            int i0 = p[j0], j1 = -1;\n            ll delta = INF;\n            for (int j = 1; j <= n; j++) {\n                if (!used[j]) {\n                    ll cur = cost[i0][j] - u[i0] - v[j];\n                    if (cur < minv[j]) { minv[j] = cur; way[j] = j0; }\n                    if (minv[j] < delta) { delta = minv[j]; j1 = j; }\n                }\n            }\n            for (int j = 0; j <= n; j++) {\n                if (used[j]) { u[p[j]] += delta; v[j] -= delta; }\n                else minv[j] -= delta;\n            }\n            j0 = j1;\n        } while (p[j0] != 0);\n        do {\n            int j1 = way[j0];\n            p[j0] = p[j1];\n            j0 = j1;\n        } while (j0 != 0);\n    }\n    vector<int> rowToCol(n+1, 0);\n    for (int j = 1; j <= n; j++) rowToCol[p[j]] = j;\n    ll total = 0;\n    for (int i = 1; i <= n; i++) total += cost[i][rowToCol[i]];\n    return {total, rowToCol};\n}\n\nint main(){\n    cin >> n;\n    cost.assign(n+1, vector<ll>(n+1));\n    for (int i = 1; i <= n; i++)\n        for (int j = 1; j <= n; j++)\n            cin >> cost[i][j];\n    auto [total, rowToCol] = hungarianMin();\n    cout << total << \"\\n\";\n    for (int i = 1; i <= n; i++) cout << rowToCol[i] << \" \\n\"[i==n];\n}\n",
          "sample_input": "3\n4 1 3\n2 0 5\n3 2 2\n",
          "expected_output": "5\n2 1 3\n"
        },
        {
          "title": "Maximum-Profit Assignment",
          "problem": "There are n agents and n contracts. You are given an n x n profit matrix where profit[i][j] is the profit earned if agent i is assigned contract j. Assign every agent exactly one distinct contract to MAXIMIZE total profit. Output the maximum total profit and one optimal assignment.\n\nInput: n, then n rows of n integers each (the profit matrix).\nOutput: the maximum total profit on the first line; then n integers on the second line, the contract assigned to each agent.",
          "approach": "Negate every entry of the profit matrix to obtain a cost matrix (cost[i][j] = -profit[i][j]), then run the exact same O(n^3) minimum-cost Hungarian algorithm on this negated matrix. Minimizing total negated-profit is equivalent to maximizing total profit, and the optimal assignment (the bijection itself) is identical either way — only the reported total needs to be negated back at the end.\n\nHand-verified on the 3x3 profit matrix [[5,9,4],[2,6,8],[7,3,1]]: brute-forcing all 6 permutations gives totals 12, 16, 12, 24, 9, 17, so the true maximum is 24, achieved uniquely by agent1->contract2 (profit 9), agent2->contract3 (profit 8), agent3->contract1 (profit 7), summing to 24 — exactly matching the program's output of total profit 24 and assignment (2, 3, 1). Note this optimum is NOT the trivial diagonal assignment, confirming the algorithm is doing genuine combinatorial work rather than defaulting to an identity matching.",
          "code": "#include <bits/stdc++.h>\nusing namespace std;\ntypedef long long ll;\nconst ll INF = LLONG_MAX / 2;\n\nint n;\nvector<vector<ll>> cost; // 1-indexed n x n, holds NEGATED profit so min-cost Hungarian maximizes profit\n\npair<ll, vector<int>> hungarianMin() {\n    vector<ll> u(n+1, 0), v(n+1, 0);\n    vector<int> p(n+1, 0), way(n+1, 0);\n    for (int i = 1; i <= n; i++) {\n        p[0] = i;\n        int j0 = 0;\n        vector<ll> minv(n+1, INF);\n        vector<bool> used(n+1, false);\n        do {\n            used[j0] = true;\n            int i0 = p[j0], j1 = -1;\n            ll delta = INF;\n            for (int j = 1; j <= n; j++) {\n                if (!used[j]) {\n                    ll cur = cost[i0][j] - u[i0] - v[j];\n                    if (cur < minv[j]) { minv[j] = cur; way[j] = j0; }\n                    if (minv[j] < delta) { delta = minv[j]; j1 = j; }\n                }\n            }\n            for (int j = 0; j <= n; j++) {\n                if (used[j]) { u[p[j]] += delta; v[j] -= delta; }\n                else minv[j] -= delta;\n            }\n            j0 = j1;\n        } while (p[j0] != 0);\n        do {\n            int j1 = way[j0];\n            p[j0] = p[j1];\n            j0 = j1;\n        } while (j0 != 0);\n    }\n    vector<int> rowToCol(n+1, 0);\n    for (int j = 1; j <= n; j++) rowToCol[p[j]] = j;\n    ll total = 0;\n    for (int i = 1; i <= n; i++) total += cost[i][rowToCol[i]];\n    return {total, rowToCol};\n}\n\nint main(){\n    cin >> n;\n    vector<vector<ll>> profit(n+1, vector<ll>(n+1));\n    cost.assign(n+1, vector<ll>(n+1));\n    for (int i = 1; i <= n; i++)\n        for (int j = 1; j <= n; j++) {\n            cin >> profit[i][j];\n            cost[i][j] = -profit[i][j]; // maximize profit == minimize negated profit\n        }\n    auto [totalNeg, rowToCol] = hungarianMin();\n    cout << (-totalNeg) << \"\\n\";\n    for (int i = 1; i <= n; i++) cout << rowToCol[i] << \" \\n\"[i==n];\n}\n",
          "sample_input": "3\n5 9 4\n2 6 8\n7 3 1\n",
          "expected_output": "24\n2 3 1\n"
        }
      ]
    }
  ]
}
